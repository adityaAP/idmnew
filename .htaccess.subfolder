<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /idm-home/
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule ^(.*)$ index.php/$1 [L,QSA]
  Options -Indexes
</IfModule>

<IfModule mod_env.c>
  SetEnv CI_ENV production
</IfModule>
